name: jphp-discord-rpc-ext

version: 1.0.0

devDeps:
  dn-bundle-plugin: '*'

plugins:
  - Doc

sources:
  - src

config:
  ignore:
    - /sandbox/**
    - /.idea/**
    - /*.iml
    - /.git/**
    - /examples/**
    - /package.hub.yml
    - /bundle/**
    - build
    - out
    - target
    - pom.xml
    - /src-bundle/**
    - /README.MD
    - /.gitignore
    - .gradle